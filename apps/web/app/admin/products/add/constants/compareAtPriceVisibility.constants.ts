/**
 * Temporary UI switch for hiding compare-at-price inputs in admin product add/edit.
 * Keep data/submit logic intact so we can re-enable UI quickly later.
 */
export const SHOW_COMPARE_AT_PRICE_FIELD = false;
